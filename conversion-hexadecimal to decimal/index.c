#include <stdio.h>
#include <math.h>

int hexToDecimal(char n)
{
}

int main()
{
    char num;
    printf("input an hexadecimal no: ");
    scanf("%c", &num);
    printf("%d", hexToDecimal(num));
}